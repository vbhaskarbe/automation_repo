
#!/bin/python

import math
import os
import random
import re
import sys

# Complete the solve function below.
def solve(s):
#    sn = ' '.join([word.capitalize() for word in s.split(' ')])
    retuurn '-'.join
    #return sn

if __name__ == '__main__':
    #fptr = open(os.environ['PWD']+'/temp.out', 'w')

    #s = raw_input()

    #result = solve(s)
    result = solve("this Text needs to 12be capitalized nicely.")
    print(result)
    #fptr.write(result + '\n')

    #fptr.close()

